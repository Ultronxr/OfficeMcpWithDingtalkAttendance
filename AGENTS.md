# 项目开发约定

- 修改或新增工具、日志、日期参数、设备调度和持久化时，必须先阅读并遵守 [全项目时间规范](docs/time-conventions.md)。北京时间格式由服务端 `OfficeTime` 与手机 `office_time.js` 统一处理，禁止各功能另行约定。
- MCP 完整时间点必须为可读的 `yyyy-MM-ddTHH:mm:ss+08:00`，包括 full details 和本地执行摘要；不添加 `time_zone`。纯日期、重复时刻和时长按规范区分，不能给 Unix 毫秒直接加八小时。
- 变更时间契约必须同步 HTTP 原始 JSON／OpenAPI 测试、旧状态兼容测试和非北京时间宿主下的脚本测试。不得用真实打卡作为自动化测试。
- 实际部署记录、凭据、员工资料和日志保留在 Git 忽略的本地文件中；提交与部署遵循用户当前明确指令。
